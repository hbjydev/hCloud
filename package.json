{
    "name": "@hcloud/root",
    "private": true,
    "scripts": {
        "build": "turbo run build",
        "dev": "turbo run dev",
        "build:watch": "turbo run build:watch"
    },
    "devDependencies": {
        "turbo": "^1.10.12"
    }
}
